/**
 * typhex/postgres — PostgreSQL-specific aggregate functions.
 * Import from "typhex/postgres" to use Postgres-only aggregates in query lambdas.
 */

export { stringAgg, arrayAgg, jsonAgg } from "./postgres/aggregates.js";
